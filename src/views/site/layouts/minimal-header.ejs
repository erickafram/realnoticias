<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= typeof metaDescription !== 'undefined' ? metaDescription : (settings.site_description || '') %>">
  
  <title><%= typeof title !== 'undefined' ? title : 'Login' %> | <%= settings.site_name || 'Portal' %></title>
  
  <!-- Favicon -->
  <link rel="icon" href="<%= settings.site_favicon || '/images/favicon.ico' %>" type="image/x-icon">
  
  <!-- Bootstrap 5.3 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.css" rel="stylesheet">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --primary-color: <%= settings.color_primary || '#3b82f6' %>;
      --primary-dark: <%= settings.color_primary_dark || '#2563eb' %>;
      --primary-light: <%= settings.color_primary || '#3b82f6' %>15;
    }
    
    * {
      font-family: 'Inter', sans-serif;
      box-sizing: border-box;
    }
    
    body {
      background: #f8fafc;
      min-height: 100vh;
      margin: 0;
    }
    
    /* Minimal Header */
    .minimal-header {
      background: #fff;
      padding: 16px 0;
      border-bottom: 1px solid #e2e8f0;
    }
    
    .minimal-header .container {
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    
    .site-logo {
      display: flex;
      align-items: center;
      gap: 10px;
      text-decoration: none;
      color: #0f172a;
      font-size: 20px;
      font-weight: 700;
    }
    
    .site-logo i {
      color: var(--primary-color);
      font-size: 24px;
    }
    
    .site-logo .logo-img {
      max-height: 36px;
      width: auto;
    }
    
    .btn-close-wizard {
      display: flex;
      align-items: center;
      gap: 6px;
      padding: 8px 16px;
      background: #f1f5f9;
      color: #64748b;
      border-radius: 8px;
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
      transition: all 0.2s;
    }
    
    .btn-close-wizard:hover {
      background: #e2e8f0;
      color: #475569;
    }
  </style>
</head>
<body>
  <!-- Minimal Header -->
  <header class="minimal-header">
    <div class="container">
      <a href="/" class="site-logo">
        <% if (settings.site_logo) { %>
          <img src="<%= settings.site_logo %>" alt="<%= settings.site_name || 'Portal' %>" class="logo-img">
        <% } else { %>
          <i class="bi bi-lightning-charge-fill"></i>
          <%= settings.site_name || 'Portal' %>
        <% } %>
      </a>
      <a href="/" class="btn-close-wizard">
        <i class="bi bi-x-lg"></i>
        <span class="d-none d-sm-inline">Fechar</span>
      </a>
    </div>
  </header>

  <!-- Content -->
  <main>
